{
  "name": "todoapp-sean",
  "version": "1.0.0",
  "scripts": {
    "install": "cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && node server.js"
  },
  "description": "Todo App with React, Node.js, PostgreSQL",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "pg": "^8.7.1",
    "if-env": "^1.0.4"
  }
}
